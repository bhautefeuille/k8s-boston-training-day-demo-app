kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /tmp/kind-control-plane
        containerPath: /tmp/kind-control-plane

  - role: worker
